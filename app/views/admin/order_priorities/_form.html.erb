<%=
active_admin_form_for [:admin, @order_priority], html: { multipart: true } do |f| 
	f.inputs "Priority Details" do
		f.input :name
        f.input :weight, :as => :select, :collection => OrderPriority::WEIGHTS, :member_label => :titleize
	end 
	f.buttons 
end 
%>