<%=
	form.has_many :artworks do |artwork|  
	  artwork.input :file, :hint => artwork.template.image_tag(artwork.object.file.url, :height => '100'), :wrapper_html => { :class => 'art-row' }
	  artwork.input :print_location_id, as: :select, :collection => PrintLocation.all, :wrapper_html => { :class => 'art-row' }, label: "Location*"
	  artwork.input :color, :wrapper_html => { :class => 'art-row' }
	end 
%>